package com.kano.sorting;/** * 选择排序 * <p> * 描述：选择排序是一种基本的排序算法，其核心思想是每次在未排序的数据中找到最小元素，然后放到已排序部分的末尾。 * 它的时间复杂度为O(n^2)，不稳定且原地排序。虽然简单，但不适合处理大规模数据。 * * @author LiuShang * @date 2023-05-27 08:35 */public class SelectionSort {    /**     * 具体实现     *     * @param nums 需要排序的数组     */    private void achieve(int[] nums) {        for (int i = 0; i < nums.length; i++) {            int minNumIdx = i;            for (int j = (i + 1); j < nums.length; j++) {                if (nums[j] < nums[minNumIdx]) {                    minNumIdx = j;                }            }            int temp = nums[i];            nums[i] = nums[minNumIdx];            nums[minNumIdx] = temp;        }    }}